# 개인정보 처리 안내

최종 수정일: 2026-09-03

`coupangctl`은 내 쿠팡 주문 기록을 내 컴퓨터에 저장하고 분석하는 로컬 우선
오픈소스 도구입니다. 쿠팡의 공식 제품이 아니며, 개발자가 운영하는 수집 서버나
분석 서버가 없습니다.

## 일반 Chrome 연결의 단일 목적

`coupangctl 일반 브라우저 연결` 확장 프로그램의 유일한 목적은 사용자가 직접
선택하고 동의한 쿠팡 주문목록 탭에서 정규화된 주문 기록을 사용자의 로컬
`coupangctl` 프로세스로 전달하는 것입니다. 툴바 아이콘을 누른 것만으로는
데이터를 읽지 않으며, 팝업의 **이 탭 연결** 버튼을 눌러야 한 번 연결합니다.

## 처리하는 데이터

선택한 주문목록 탭에서 다음 필드가 있을 때만 처리합니다.

- 해시 처리한 주문 참조값, 주문 날짜·시간
- 주문 금액, 할인액, 배송비, 통화
- 상품의 공개 숫자 ID, 상품명, 브랜드·판매자 표시명
- 수량, 취소·반품 수량, 표시 단가·결제액
- 배송 상태·배송 완료 시각, 영수증 제공 여부
- 다음 주문 페이지를 읽기 위한 연도·페이지 위치

확장은 탭 메모리 안에서 쿠팡 주문 화면의 구조화 데이터를 검사해 위 필드만
정규화합니다. 원본 문서나 원본 응답은 확장 밖으로 전달하거나 저장하지 않습니다.
추가 페이지가 필요하면 같은 `mc.coupang.com` 주문목록 origin에 현재 브라우저
세션으로 제한된 읽기 요청을 보냅니다.

다음 데이터는 읽거나 전달하지 않습니다.

- 쿠키, 로그인 토큰, 비밀번호, OTP
- 주소, 전화번호, 이메일, 전체 카드·계좌 번호
- 다른 탭의 내용이나 전체 방문 기록
- 원본 주문 payload나 원본 페이지 문서

## 데이터 흐름과 보관

정규화된 주문 기록은 Chrome Native Messaging을 통해 같은 컴퓨터의
`coupangctl` 바이너리로 전달되고, 인증된 일회성 로컬 연결을 거쳐 사용자 상태
디렉터리의 `coupangctl.sqlite3`에 저장됩니다. 확장 프로그램 자체는 주문 결과를
저장하지 않습니다.

개발자 또는 제3자 서버로 데이터를 전송하지 않으며, 판매·광고·프로파일링·신용
평가에 사용하지 않습니다. telemetry와 원격 코드를 사용하지 않습니다. 사용자가
별도로 실행한 쿠팡 상품 검색 같은 다른 기능의 네트워크 요청은 해당 명령의
목적에 한정되며 이 확장 프로그램이 수집 서버를 운영한다는 뜻이 아닙니다.

## 권한을 사용하는 이유

- `activeTab`: 사용자가 현재 선택하고 직접 승인한 탭에만 임시로 접근합니다.
- `scripting`: 패키지에 포함된 검토된 reader를 선택한 top frame에서 한 번
  실행합니다.
- `nativeMessaging`: 정규화된 결과를 같은 컴퓨터의 `coupangctl`로 전달합니다.

광범위한 host permission, `cookies`, `storage`, 외부 메시지 권한은 요청하지
않습니다.

## 사용자의 선택과 삭제

연결하지 않으려면 팝업을 닫으면 됩니다. 확장은 Chrome에서 언제든 제거할 수
있고, 로컬 브리지는 다음 명령으로 제거할 수 있습니다.

```bash
coupangctl browser-bridge uninstall
```

정규화된 주문·상품 행은 명시적인 확인 문자열이 필요한 다음 명령으로 삭제할 수
있습니다.

```bash
coupangctl orders purge --confirm purge-normalized-orders
```

가격 관측, 계정 요약 등 로컬 상태 전체를 제거하려면 먼저 필요한 내보내기를 한
뒤 운영체제별 사용자 상태 디렉터리의 `coupangctl` 폴더를 사용자가 직접 삭제해야
합니다. 브리지 제거는 Chrome 프로필이나 로컬 주문 DB를 자동으로 삭제하지
않습니다.

## 문의와 변경

코드와 데이터 경로는 공개 저장소에서 검토할 수 있습니다. 문의 또는 개인정보
관련 요청은
[GitHub Issues](https://github.com/JungHoonGhae/coupang-ctl/issues)에 남겨 주세요.
처리 범위가 바뀌면 이 문서의 최종 수정일과 Chrome Web Store 공개 내용을 함께
갱신합니다.
